spring:
  application:
    name: notification-service
  profiles:
    active: dev

  # Database configuration (can be overridden by environment variables)
  datasource:
    url: jdbc:postgresql://174.138.17.214:5432/notification_service
    username: kithya
    password: DE6Y6S30XO3KPTH
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

  kafka:
    bootstrap-servers: kafka:9092
    consumer:
      group-id: notification-gateway-group
      auto-offset-reset: earliest
      enable-auto-commit: false
    producer:
      acks: all
      retries: 3

server:
  port: 8080

jwt:
  secret: da2bd42bef8f9f957d647ed13571dadd5b59c7055fb723eacdb05154878da17d77d3180385fc232cab38bded0fc7dbda0d439cc275e91164ade266d64d50633bc7
  expiration: 99999999999999999

app:
  init:
    create-admin: true
    admin-email: phatmenghor19@gmail.com
    admin-password: 88889999

notification:
  gateway:
    enabled: true
    max-recipients-per-request: 100
  kafka:
    topics:
      telegram: telegram-notifications
      email: email-notifications
      logs: notification-logs
  telegram:
    api-url: https://api.telegram.org/bot
    timeout: 5000
  email:
    timeout: 10000

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

# Logging configuration
logging:
  level:
    root: INFO
    com.emenu: INFO
    org.springframework.web: INFO
    org.springframework.kafka: INFO
    org.hibernate.SQL: OFF
    org.hibernate.type.descriptor.sql.BasicBinder: OFF
    org.hibernate.orm.jdbc.bind: OFF